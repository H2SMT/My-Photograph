<!DOCTYPE html>
<html>
  <head>
    <title>MyPhotograph</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/08c0548ff0.js" crossorigin="anonymous"></script>
  </head>

  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <!--ロゴ-->
          <a class="navbar-brand p-3" href="/post_images"><%= image_tag('logo.png', size: '50x50') %> Myphotograph</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'btn btn-secondary' %>
            </li>
            <li class="nav-item">
              <%= link_to '投稿一覧', post_images_path, class: "btn btn-secondary" %>
            </li>
            <li class="nav-item">
              <%= link_to 'New Post', new_post_image_path, class: "btn btn-secondary" %>
            </li>
            <li class="nav-item">
              <%= link_to 'マイページ', (user_path(current_user.id)), class: 'btn btn-secondary' %>
            </li>
            <% else %>
            <li class="nav-item">
              <%= link_to "新規登録", new_user_registration_path, class: 'btn btn-secondary' %>
            </li>
            <li class="nav-item">
              <%= link_to "ログイン", new_user_session_path, class: 'btn btn-secondary' %>
            </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main class="mb-auto">
    <%= yield %>
    </main>
    <footer class="bg-dark text-white pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-5">
          </div>
          <div class="col-md-7">
            <h6 class="text-white mt-2 mb-3">Copyright © Obara Taiki</h6>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
